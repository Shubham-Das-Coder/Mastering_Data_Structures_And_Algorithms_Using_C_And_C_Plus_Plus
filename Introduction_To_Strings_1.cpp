#include <stdio.h>

int main()
{
    char ch;          // Creating a character variale
    ch = 'A';         // Initializing the character variable
    printf("%c", ch); // Printing the character

    return 0;
}