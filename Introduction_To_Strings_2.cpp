#include <stdio.h>

int main()
{
    char ch;          // Creating a character variable
    ch = 'A';         // Initializing the character variable
    printf("%d", ch); // Printing the ASCII value of the character

    return 0;
}